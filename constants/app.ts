/**
 * App config for v1 (offline-only) deployment.
 * Set BACKEND_ENABLED to true in v2 when you integrate your backend API.
 */
export const BACKEND_ENABLED = false;

/** All data is stored locally (AsyncStorage). No server required. */
export const OFFLINE_FIRST = true;
